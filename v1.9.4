local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

pcall(function()
    local g = getgenv and getgenv() or _G
    if g.debug then g.debug.info, g.debug.getupvalue, g.debug.getconstant = function() end, function() end, function() end end
    local hook = hookmetamethod or detach
    if hook then
        local oIdx, oNc
        oIdx = hook(game, "__index", function(self, k) return (not checkcaller() and (k == "WalkSpeed" or k == "JumpPower")) and 16 or oIdx(self, k) end)
        oNc = hook(game, "__namecall", function(self, ...)
            local m, n = getnamecallmethod(), tostring(self.Name):lower()
            if (m == "FireServer" or m == "InvokeServer") and (n:find("ban") or n:find("cheat") or n:find("validator")) then return nil end
            return oNc(self, ...)
        end)
    end
end)

local P, L, TS, RS, RSvc, UIS = game:GetService("Players"), game:GetService("Lighting"), game:GetService("TweenService"), game:GetService("ReplicatedStorage"), game:GetService("RunService"), game:GetService("UserInputService")
local LP, Cam = P.LocalPlayer, workspace.CurrentCamera

-- Anti-AFK
LP.Idled:Connect(function()
    local bb = game:GetService("VirtualUser")
    bb:CaptureController()
    bb:ClickButton2(Vector2.new())
end)

local AutoFarm, BringMob, FastAttack, FastDistance, AutoBones, AutoKatakuri, AutoBoss = false, false, false, false, false, false, false
local AutoChestFullMap = false
local AutoStatsMelee, AutoStatsDefense, AutoStatsSword, AutoStatsFruit = false, false, false, false
local ESPBox, ESPName, ESPDist, ESPLine, ESPFruit, WalkOnWater, SpeedHack, WalkSpeedValue, InfJump = false, false, false, false, false, false, false, 32, false
local CurTween, CurPos, WaterPart

-- Bảng ghi nhớ rương đã nhặt
local CollectedChests = {}

local Quests = {
    {1, "BanditQuest1", 1, "Bandit", CFrame.new(1059, 16, 1549), CFrame.new(1145, 16, 1630)},
    {10, "JungleQuest", 1, "Monkey", CFrame.new(-1598, 36, 153), CFrame.new(-1448, 50, 63)},
    {15, "JungleQuest", 2, "Gorilla", CFrame.new(-1598, 36, 153), CFrame.new(-1237, 6, -486)},
    {30, "PirateQuest", 1, "Pirate", CFrame.new(-1140, 4, 3828), CFrame.new(-1200, 4, 3900)},
    {60, "DesertQuest", 1, "Desert Bandit", CFrame.new(894, 6, 4388), CFrame.new(950, 6, 4450)},
    {90, "SnowQuest", 1, "Snow Bandit", CFrame.new(1385, 87, -1298), CFrame.new(1300, 87, -1350)}
}

local function GetLvl(p) local target, lvl = p or LP, 1 pcall(function() lvl = target:FindFirstChild("Data") and target.Data.Level.Value or target.Level.Value end) return lvl end
local function GetQ() local l, c = GetLvl(), Quests[1] for _, q in ipairs(Quests) do if l >= q[1] then c = q end end return {QName = c[2], QNum = c[3], MName = c[4], Npc = c[5], Mob = c[6]} end

local function FlyTo(targetCF, speed)
    speed = speed or 250
    local chr = LP.Character
    local hrp, hum = chr and chr:FindFirstChild("HumanoidRootPart"), chr and chr:FindFirstChildOfClass("Humanoid")
    if not hrp or not hum then return end
    hrp.Velocity = Vector3.zero
    local dist = (hrp.Position - targetCF.Position).Magnitude
    if dist > 6 then
        hum.PlatformStand = true
        for _, v in pairs(chr:GetChildren()) do if v:IsA("BasePart") then v.CanCollide = false end end
        if not CurPos or (CurPos - targetCF.Position).Magnitude > 3 then
            CurPos = targetCF.Position
            if CurTween then CurTween:Cancel() end
            CurTween = TS:Create(hrp, TweenInfo.new(dist / speed, Enum.EasingStyle.Linear), {CFrame = targetCF})
            CurTween:Play()
        end
    else
        if CurTween then CurTween:Cancel(); CurTween = nil end
        CurPos, hrp.CFrame, hum.PlatformStand = nil, targetCF, false
    end
end

local function StopFly()
    if CurTween then CurTween:Cancel(); CurTween = nil end
    CurPos = nil
    local chr = LP.Character
    local hum = chr and chr:FindFirstChildOfClass("Humanoid")
    if hum then hum.PlatformStand = false end
    if chr then
        for _, v in pairs(chr:GetChildren()) do if v:IsA("BasePart") then v.CanCollide = true end end
    end
end

-- Vòng lặp Auto Nhặt Rương Full Map (Không nhặt lại rương cũ)
task.spawn(function()
    while true do
        task.wait(0.1)
        if AutoChestFullMap then
            pcall(function()
                local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end
                
                local closestChest = nil
                local shortestDist = math.huge
                
                for _, v in pairs(workspace:GetDescendants()) do
                    -- Kiểm tra xem vật thể có phải rương không và CHƯA nằm trong danh sách đã nhặt
                    if v:IsA("Part") and v.Name:find("Chest") and v:IsDescendantOf(workspace) and not CollectedChests[v] then
                        local dist = (v.Position - hrp.Position).Magnitude
                        if dist < shortestDist then
                            shortestDist = dist
                            closestChest = v
                        end
                    end
                end

                if closestChest and closestChest.Parent then
                    if shortestDist > 5 then
                        FlyTo(closestChest.CFrame, 300)
                    else
                        StopFly()
                        hrp.CFrame = closestChest.CFrame
                        firetouchinterest(hrp, closestChest, 0)
                        task.wait(0.05)
                        firetouchinterest(hrp, closestChest, 1)
                        
                        -- Đánh dấu rương đã nhặt thành công
                        CollectedChests[closestChest] = true
                        task.wait(0.1)
                    end
                else
                    StopFly()
                end
            end)
        end
    end
end)

-- Vòng lặp Tấn công
task.spawn(function()
    while true do
        if AutoFarm or AutoBones or AutoKatakuri or AutoBoss then
            pcall(function()
                local chr = LP.Character
                if chr and chr:FindFirstChild("Humanoid") and chr.Humanoid.Health > 0 then
                    local tool = chr:FindFirstChildOfClass("Tool")
                    if not tool then
                        for _, t in pairs(LP.Backpack:GetChildren()) do
                            if t:IsA("Tool") and t.ToolTip ~= "Gun" then 
                                chr.Humanoid:EquipTool(t) 
                                break 
                            end 
                        end
                    else
                        tool:Activate()
                        if RS:FindFirstChild("Modules") and RS.Modules:FindFirstChild("Net") then
                            pcall(function() RS.Modules.Net:FindFirstChild("RegisterAttack"):InvokeServer() end)
                        end
                        if FastDistance and tool:FindFirstChild("Handle") then 
                            tool.Handle.Size = Vector3.new(50, 50, 50)
                            tool.Handle.CanCollide = false 
                        end
                    end
                end
            end)
        end
        task.wait(FastAttack and 0.015 or 0.2)
    end
end)

-- Logic Auto Farm Level
RSvc.Heartbeat:Connect(function()
    if AutoFarm then
        pcall(function()
            local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            hrp.Velocity = Vector3.zero
            
            local q, qGui = GetQ(), LP.PlayerGui.Main:FindFirstChild("Quest")
            local hasQ = qGui and qGui.Visible and qGui.Container.QuestTitle.Title.Text ~= ""
            
            if not hasQ then
                if (hrp.Position - q.Npc.Position).Magnitude > 10 then 
                    FlyTo(q.Npc, 250)
                else 
                    StopFly()
                    hrp.CFrame = q.Npc 
                    RS.Remotes.CommF_:InvokeServer("StartQuest", q.QName, q.QNum) 
                end
            else
                local targetMob = nil
                local shortestDist = math.huge
                
                if workspace:FindFirstChild("Enemies") then
                    for _, v in pairs(workspace.Enemies:GetChildren()) do
                        if (v.Name == q.MName or v.Name:find(q.MName)) and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 and v:FindFirstChild("HumanoidRootPart") then
                            local d = (v.HumanoidRootPart.Position - hrp.Position).Magnitude
                            if d < shortestDist then
                                shortestDist = d
                                targetMob = v
                            end
                        end
                    end
                end
                
                if targetMob and targetMob:FindFirstChild("HumanoidRootPart") then
                    StopFly()
                    local mHrp = targetMob.HumanoidRootPart
                    hrp.CFrame = CFrame.new(mHrp.Position + Vector3.new(0, 10, 0), mHrp.Position)
                    if BringMob and workspace:FindFirstChild("Enemies") then
                        for _, om in pairs(workspace.Enemies:GetChildren()) do
                            if (om.Name == q.MName or om.Name:find(q.MName)) and om:FindFirstChild("HumanoidRootPart") and om:FindFirstChild("Humanoid") and om.Humanoid.Health > 0 then
                                if (om.HumanoidRootPart.Position - mHrp.Position).Magnitude < 250 then
                                    om.HumanoidRootPart.CFrame = mHrp.CFrame
                                    om.HumanoidRootPart.CanCollide = false
                                end
                            end
                        end
                    end
                else
                    FlyTo(q.Mob, 250)
                end
            end
        end)
    end
end)

-- Auto Add Stats
task.spawn(function()
    while task.wait(0.5) do
        pcall(function()
            if AutoStatsMelee then RS.Remotes.CommF_:InvokeServer("AddPoint", "Melee", 3) end
            if AutoStatsDefense then RS.Remotes.CommF_:InvokeServer("AddPoint", "Defense", 3) end
            if AutoStatsSword then RS.Remotes.CommF_:InvokeServer("AddPoint", "Sword", 3) end
            if AutoStatsFruit then RS.Remotes.CommF_:InvokeServer("AddPoint", "Demon Fruit", 3) end
        end)
    end
end)

-- ESP System
local ESP_Cache = {}
local function CreateESP(plr)
    if plr == LP or ESP_Cache[plr] then return end
    local line, box, bg, txt = Drawing.new("Line"), Instance.new("Highlight"), Instance.new("BillboardGui"), Instance.new("TextLabel")
    line.Visible, line.Color, line.Thickness = false, Color3.fromRGB(0, 255, 255), 1.5
    box.Name, box.FillColor, box.OutlineColor, box.FillTransparency, box.Enabled = "ESP_Box", Color3.fromRGB(255, 50, 50), Color3.fromRGB(255, 255, 255), 0.5, false
    bg.Name, bg.Size, bg.AlwaysOnTop, bg.StudsOffset, bg.Enabled = "ESP_Text", UDim2.new(0, 200, 0, 50), true, Vector3.new(0, 3.5, 0), false
    txt.Parent, txt.Size, txt.BackgroundTransparency, txt.TextColor3, txt.TextSize, txt.Font, txt.TextStrokeTransparency = bg, UDim2.new(1, 0, 1, 0), 1, Color3.fromRGB(0, 255, 255), 13, Enum.Font.SourceSansBold, 0
    ESP_Cache[plr] = {Line = line, Box = box, Billboard = bg, TextLabel = txt}
end

for _, plr in pairs(P:GetPlayers()) do CreateESP(plr) end
P.PlayerAdded:Connect(CreateESP)
P.PlayerRemoving:Connect(function(p) if ESP_Cache[p] then pcall(function() ESP_Cache[p].Line:Remove() ESP_Cache[p].Box:Destroy() ESP_Cache[p].Billboard:Destroy() end) ESP_Cache[p] = nil end end)

RSvc.RenderStepped:Connect(function()
    local myHrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    for plr, data in pairs(ESP_Cache) do
        local char = plr.Character
        local pHrp, pHum = char and char:FindFirstChild("HumanoidRootPart"), char and char:FindFirstChildOfClass("Humanoid")
        if char and pHrp and pHum and pHum.Health > 0 then
            data.Box.Enabled = ESPBox
            if data.Box.Parent ~= char then data.Box.Parent = char end
            if ESPName or ESPDist then
                if data.Billboard.Parent ~= pHrp then data.Billboard.Parent = pHrp end
                data.TextLabel.Text, data.Billboard.Enabled = (ESPName and plr.Name or "") .. (ESPDist and myHrp and "\n[" .. math.floor((pHrp.Position - myHrp.Position).Magnitude) .. "m]" or ""), true
            else data.Billboard.Enabled = false end
            if ESPLine and myHrp then
                local pos, vis = Cam:WorldToViewportPoint(pHrp.Position)
                if vis then data.Line.From, data.Line.To, data.Line.Visible = Vector2.new(Cam.ViewportSize.X / 2, Cam.ViewportSize.Y), Vector2.new(pos.X, pos.Y), true else data.Line.Visible = false end
            else data.Line.Visible = false end
        else data.Box.Enabled, data.Billboard.Enabled, data.Line.Visible = false, false, false end
    end
end)

RSvc.RenderStepped:Connect(function()
    pcall(function()
        local myHrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        for _, v in pairs(workspace:GetChildren()) do
            if v:IsA("Tool") or v.Name:find("Fruit") then
                local handle = v:FindFirstChild("Handle") or v:FindFirstChildWhichIsA("BasePart")
                if handle then
                    local bg = handle:FindFirstChild("FruitESP")
                    if ESPFruit then
                        if not bg then
                            bg = Instance.new("BillboardGui", handle)
                            bg.Name, bg.Size, bg.AlwaysOnTop, bg.StudsOffset = "FruitESP", UDim2.new(0, 220, 0, 50), true, Vector3.new(0, 2.5, 0)
                            local txt = Instance.new("TextLabel", bg)
                            txt.Name, txt.Size, txt.BackgroundTransparency, txt.TextColor3, txt.TextSize, txt.Font, txt.TextStrokeTransparency = "Txt", UDim2.new(1, 0, 1, 0), 1, Color3.fromRGB(255, 170, 0), 14, Enum.Font.SourceSansBold, 0
                        end
                        bg.Txt.Text, bg.Enabled = "🍎 " .. v.Name .. (myHrp and (" [" .. math.floor((handle.Position - myHrp.Position).Magnitude) .. "m]") or ""), true
                    elseif bg then bg.Enabled = false end
                end
            end
        end
    end)
end)

local function TeleportToFruit()
    pcall(function()
        local fruits = {}
        for _, v in pairs(workspace:GetChildren()) do
            if v:IsA("Tool") or v.Name:find("Fruit") then
                local handle = v:FindFirstChild("Handle") or v:FindFirstChildWhichIsA("BasePart")
                if handle then table.insert(fruits, {Tool = v, Part = handle}) end
            end
        end
        if #fruits > 0 then
            local myHrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
            if myHrp then
                table.sort(fruits, function(a, b) return (a.Part.Position - myHrp.Position).Magnitude < (b.Part.Position - myHrp.Position).Magnitude end)
                local target = fruits[1]
                task.spawn(function()
                    while target.Part and target.Part.Parent and (target.Part.Position - myHrp.Position).Magnitude > 5 do FlyTo(target.Part.CFrame, 300) task.wait(0.05) end
                    StopFly()
                    if target.Part and target.Part.Parent then myHrp.CFrame = target.Part.CFrame firetouchinterest(myHrp, target.Part, 0) task.wait(0.1) firetouchinterest(myHrp, target.Part, 1) end
                end)
            end
        end
    end)
end

RSvc.RenderStepped:Connect(function()
    pcall(function()
        local chr = LP.Character
        local hum, hrp = chr and chr:FindFirstChildOfClass("Humanoid"), chr and chr:FindFirstChild("HumanoidRootPart")
        if SpeedHack and hum then hum.WalkSpeed = WalkSpeedValue end
        if WalkOnWater and hrp then
            if not WaterPart or not WaterPart.Parent then
                WaterPart = Instance.new("Part", workspace)
                WaterPart.Name, WaterPart.Size, WaterPart.Anchored, WaterPart.Transparency, WaterPart.Color = "WaterPlatform", Vector3.new(300, 1, 300), true, 0.5, Color3.fromRGB(0, 170, 255)
            end
            WaterPart.CFrame = CFrame.new(hrp.Position.X, 0, hrp.Position.Z)
        elseif WaterPart then WaterPart:Destroy(); WaterPart = nil end
    end)
end)

UIS.JumpRequest:Connect(function() if InfJump then local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid") if hum then hum:ChangeState("Jumping") end end end)

-- Rayfield UI Setup
local Window = Rayfield:CreateWindow({
   Name = "Andz Dev Hub",
   LoadingTitle = "Andz Dev Hub",
   LoadingSubtitle = "by Andz Dev",
   ConfigurationSaving = { Enabled = false },
   KeySystem = false
})

local TabMainFarm = Window:CreateTab("Chức Năng Farm", 4483362458)
local TabFruit = Window:CreateTab("Fruit System", 4483362458)
local TabESP = Window:CreateTab("ESP Player", 4483362458)
local TabMisc = Window:CreateTab("Misc", 4483362458)

-- Tab Chức Năng Farm -> Section 1: Farm Level
TabMainFarm:CreateSection("Farm Cấp Độ")
TabMainFarm:CreateToggle({
   Name = "Auto Farm Level",
   CurrentValue = false,
   Callback = function(v) AutoFarm = v if not v then StopFly() end end,
})
TabMainFarm:CreateToggle({
   Name = "Gom Quái (Bring Mob)",
   CurrentValue = false,
   Callback = function(v) BringMob = v end,
})
TabMainFarm:CreateToggle({
   Name = "Đánh Nhanh (Fast Attack)",
   CurrentValue = false,
   Callback = function(v) FastAttack = v end,
})
TabMainFarm:CreateToggle({
   Name = "Mở Rộng Hitbox",
   CurrentValue = false,
   Callback = function(v) FastDistance = v end,
})

-- Tab Chức Năng Farm -> Section 2: Farm Khác
TabMainFarm:CreateSection("Farm Nâng Cao")
TabMainFarm:CreateToggle({
   Name = "Auto Farm Rương (Full Map)",
   CurrentValue = false,
   Callback = function(v) AutoChestFullMap = v if not v then StopFly() end end,
})
TabMainFarm:CreateToggle({
   Name = "Auto Farm Xương",
   CurrentValue = false,
   Callback = function(v) AutoBones = v end,
})
TabMainFarm:CreateToggle({
   Name = "Auto Katakuri",
   CurrentValue = false,
   Callback = function(v) AutoKatakuri = v end,
})
TabMainFarm:CreateToggle({
   Name = "Auto Farm Boss",
   CurrentValue = false,
   Callback = function(v) AutoBoss = v end,
})

-- Tab Chức Năng Farm -> Section 3: Auto Stats
TabMainFarm:CreateSection("Auto Stats")
TabMainFarm:CreateToggle({
   Name = "Tự Động Cộng Melee",
   CurrentValue = false,
   Callback = function(v) AutoStatsMelee = v end,
})
TabMainFarm:CreateToggle({
   Name = "Tự Động Cộng Defense",
   CurrentValue = false,
   Callback = function(v) AutoStatsDefense = v end,
})
TabMainFarm:CreateToggle({
   Name = "Tự Động Cộng Sword",
   CurrentValue = false,
   Callback = function(v) AutoStatsSword = v end,
})
TabMainFarm:CreateToggle({
   Name = "Tự Động Cộng Demon Fruit",
   CurrentValue = false,
   Callback = function(v) AutoStatsFruit = v end,
})

-- Tab Fruit
TabFruit:CreateSection("Trái Ác Quỷ")
TabFruit:CreateToggle({
   Name = "ESP Trái Ác Quỷ",
   CurrentValue = false,
   Callback = function(v) ESPFruit = v end,
})
TabFruit:CreateButton({
   Name = "🚀 Bay Đến Nhặt Trái",
   Callback = TeleportToFruit,
})
TabFruit:CreateButton({
   Name = "🎰 Random Trái",
   Callback = function() RS.Remotes.CommF_:InvokeServer("Cousin", "Buy") end,
})

-- Tab ESP
TabESP:CreateSection("ESP Player")
TabESP:CreateToggle({
   Name = "ESP Box",
   CurrentValue = false,
   Callback = function(v) ESPBox = v end,
})
TabESP:CreateToggle({
   Name = "ESP Tên",
   CurrentValue = false,
   Callback = function(v) ESPName = v end,
})
TabESP:CreateToggle({
   Name = "ESP Khoảng Cách",
   CurrentValue = false,
   Callback = function(v) ESPDist = v end,
})
TabESP:CreateToggle({
   Name = "ESP Đường Kẻ",
   CurrentValue = false,
   Callback = function(v) ESPLine = v end,
})

-- Tab Misc
TabMisc:CreateSection("Tiện Ích")
TabMisc:CreateToggle({
   Name = "Hack Tốc Độ",
   CurrentValue = false,
   Callback = function(v) SpeedHack = v end,
})
TabMisc:CreateSlider({
   Name = "Tốc Độ",
   Range = {16, 300},
   Increment = 1,
   Suffix = " Speed",
   CurrentValue = 32,
   Callback = function(v) WalkSpeedValue = v end,
})
TabMisc:CreateToggle({
   Name = "Nhảy Vô Hạn",
   CurrentValue = false,
   Callback = function(v) InfJump = v end,
})
TabMisc:CreateToggle({
   Name = "Đi Trên Nước",
   CurrentValue = false,
   Callback = function(v) WalkOnWater = v end,
})
